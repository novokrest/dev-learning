<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
			 x:Class="XamarinLearning.DataBinding.StringFormatDemoPage">
	<StackLayout>
		<StackLayout VerticalOptions="CenterAndExpand">
			<Label Text="{Binding Value, Source={x:Reference slider}, StringFormat='The slider value is {0:F3}'}"/>
			<Slider x:Name="slider"/>
		</StackLayout>

		<StackLayout VerticalOptions="CenterAndExpand">
			<Label Text="{Binding Text, Source={x:Reference entry}, StringFormat='The entry text is &#x201C;{0}&#x201D;'}"/>
			<Entry x:Name="entry"/>
		</StackLayout>
		
		<StackLayout VerticalOptions="CenterAndExpand">
			<Label Text="{Binding IsToggled, Source={x:Reference switch}, StringFormat='The switch value is {0}'}"/>
			<Switch x:Name="switch"/>
		</StackLayout>
		
		<StackLayout VerticalOptions="CenterAndExpand">
			<Label Text="{Binding Value, Source={x:Reference stepper}, StringFormat='The stepper value is {0}'}"/>
			<Stepper x:Name="stepper"/>
		</StackLayout>
	</StackLayout>
</ContentPage>
